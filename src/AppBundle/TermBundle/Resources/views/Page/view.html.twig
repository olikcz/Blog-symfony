{% extends '@App/Layout/layout-two-col-4-8.html.twig' %}

{% block content %}
    <div class="block block--page-view">
        <div class="block block-content">
            <h2 class="field field-name">{{ term.name }}</h2>
            <div class="field field-description">{{ term.description }}</div>
        </div>
    </div>

{%  endblock %}